#include "main.h"

void div_(long int num_1, long int num_2) {
    printf(
        "Div of %ld and %ld is equal to: %.2f\n",
        num_1,
        num_2,
        (float) num_1 / (float) num_2
    );
}
